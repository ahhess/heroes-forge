swarm:
  datasources:
    data-sources:
    #   heroesDS:
    #     driver-name: postgresql
    #     connection-url: ${JDBC_DATABASE_URL}
    #     user-name: ${JDBC_DATABASE_USERNAME}
    #     password: ${JDBC_DATABASE_PASSWORD}
    # jdbc-drivers:
    #   postgresql:
    #     driver-class-name: org.postgresql.Driver
    #     driver-module-name: org.postgresql
    #     # xa-datasource-name: org.postgresql.JdbcDataSource
